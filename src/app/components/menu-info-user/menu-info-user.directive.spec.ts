import { MenuInfoUserDirective } from './menu-info-user.directive';

describe('MenuInfoUserDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuInfoUserDirective();
    expect(directive).toBeTruthy();
  });
});
